import React from "react";
import { useApiClient, useEffect } from "../../util/customhooks";

const Orders = () => {
	return (
		<div className="container p-4">
			<h1 className="mb-4 text-2xl font-semibold">Rendelések</h1>
		</div>
	);
};

export default Orders;
